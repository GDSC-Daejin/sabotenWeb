import styled from "styled-components";
import Navbar from "../Navbar";

const Header = () => {
    return (
        <header>
            <div>
               <Navbar/>
            </div>
        </header>
    )
}
export default Header